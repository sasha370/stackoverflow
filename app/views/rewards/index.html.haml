%h2.text-center
  Ваши награды
%hr/
.row
  - @rewards.each do |reward|
    .col-3
      .card.text-center
        .card-header
          = reward.question.title
        .card-body
          = reward.title
          = image_tag url_for(reward.image), size: "70", alt: "#{reward.image.filename}"
